#! /usr/bin/env sh

mkdir -p /etc/sitegen
mkdir -p ~/.sitegen/hooks.d
cp -r sitegen/ /etc/sitegen/
cp sitegen.sh /usr/local/bin/sitegen
cp generate-ssl-cert.sh /usr/local/bin/generate-ssl-cert
cp sitegen.completion /etc/bash_completion.d/sitegen
